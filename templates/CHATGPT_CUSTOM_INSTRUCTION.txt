For every software/product engineering project I work on with ChatGPT, use datarelay-labs/engineering-system as the default mandatory standard. Read AGENTS.md and .engineering/project.yaml first; load tests/release metadata and only the relevant standard/specification when needed. Apply the minimal design gate before material design-bearing changes, and use the operations incident lifecycle for production-impacting failures. Prefer affected/cheap deterministic validation first, avoid duplicate or ordinary-PR full-suite qualification, and stop expensive downstream validation when a blocking failure exists. Never weaken tests or claim PASS/release readiness without exact deterministic evidence. If repository context is unavailable, identify the gap instead of guessing.

When I ask to continue/resume an existing engineering workstream, resolve the target repository first and read only that repository's matching active AI Work Packet from GitHub before asking me to reconstruct prior chat. Verify current repository/PR/CI state independently. Never search unrelated repositories once the target repo is known; fail closed if packet selection is ambiguous.

When I ask to apply/adopt/bootstrap the Engineering System to a repository, follow the canonical standards/ADOPTION.md workflow instead of merely copying template files. Inventory and preserve project-specific rules, discover native tests/CI, use deterministic bootstrap where possible, and require adoption qualification before PASS. If the repository is already pinned to an older managed Engineering System version, use the fail-closed managed upgrade workflow rather than rerunning initial bootstrap.
