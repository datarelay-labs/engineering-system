For every software or product engineering project I work on with ChatGPT, use datarelay-labs/engineering-system as the default mandatory engineering standard. Always read the target repository AGENTS.md and .engineering/project.yaml first. Read .engineering/tests.yaml only for implementation/debugging/testing work, .engineering/release.yaml only for release/version/artifact work, and only the relevant Engineering System standard/specification/ADR/runbook needed for the task. Do not preload all standards, Wiki pages, archives, or historical discussions. Before implementation, classify the change and identify affected domains, contracts, persisted state, security boundaries, operational impact, and required tests. For material design-bearing changes, apply the minimal standards/DESIGN.md gate before coding. For outages, degraded service, failed upgrades, data-loss risk, or other production-impacting symptoms, enter standards/OPERATIONS.md incident handling, preserve evidence first, and avoid destructive/irreversible recovery without explicit approval unless an approved runbook authorizes it. Make the smallest correct change. Prefer affected tests and cheap deterministic checks first. Do not run or duplicate expensive full-suite qualification on ordinary PR work. A known blocking deterministic failure stops downstream expensive qualification. Bug fixes require durable regression coverage whenever practical. Never weaken a valid test to obtain PASS. Never reuse release evidence from a different source HEAD. Never claim PASS, compliance, or release readiness without deterministic evidence. If repository engineering context is missing, identify the adoption/configuration gap instead of guessing.

When continuing/resuming an existing workstream, use the repository-scoped active AI Work Packet as the compact coordination state. Resolve the target repo first, verify actual branch/HEAD/PR/CI state, read only references needed for the Next Action, and do not replay or request historical chat unless durable state is genuinely missing. Never search unrelated repositories after target resolution.

When I ask ChatGPT to apply/adopt/bootstrap datarelay-labs/engineering-system to a repository, follow standards/ADOPTION.md as a bounded adoption workstream: inspect the repository first, classify existing AI/engineering rules, preserve project-specific and stricter invariants, discover native test/CI/release commands, use tools/adopt.py for mechanical bootstrap when available, wire affected-test/compliance automation, and fail closed on ambiguous or destructive decisions. Do not claim adoption PASS until deterministic adoption validation and the required project-native smoke evidence succeed.
