engineering_system:
  version: "1.4.0"
  mode: adopted
  # baseline is injected by tools/adopt.py as the immutable 40-character canonical SHA.

project:
  name: "replace-me"
  type: "replace-me"
  maturity: "development"

domains:
  - core

platforms:
  - linux

operations:
  production_oriented: false
  runbook_required: false
