For every software/product engineering project I work on with Cursor, use datarelay-labs/engineering-system as the default mandatory engineering standard. Always read repository AGENTS.md and .engineering/project.yaml first. Read .engineering/tests.yaml for implementation/debugging/testing, .engineering/release.yaml for release/version/artifact work, and only the relevant standard/specification/ADR/runbook needed for the task. Do not preload all standards or historical knowledge. Classify the change, identify affected domains/contracts/security/operations, make the smallest correct change, and run the cheapest affected deterministic tests first. Do not duplicate equivalent native CI or run a multi-hour full suite on an ordinary PR. A known blocking deterministic failure stops expensive downstream qualification. Add durable regression coverage for bugs whenever practical. Never weaken valid tests or claim PASS/release readiness from unexecuted, historical, or different-HEAD evidence. Repository .cursor/rules/engineering-system.mdc with alwaysApply: true is mandatory when present.

When I ask Cursor to continue/resume existing engineering work, resolve the current repository and branch first, load only the matching repository-scoped active AI Work Packet, verify current HEAD/state independently, and continue from its Next Action. If repository AGENTS.md or .engineering/* adoption files are absent, report ENGINEERING_SYSTEM_ADOPTION=ABSENT_OR_PENDING and continue under the canonical datarelay-labs/engineering-system default unless the active Work Packet requires adoption work. Do not create or merge adoption files implicitly. Do not scan unrelated repositories or ask me to paste prior handoff/chat when durable Work Packet state exists. Fail closed on missing or ambiguous packets.

When I ask Cursor to apply/adopt/bootstrap datarelay-labs/engineering-system, follow standards/ADOPTION.md as a separate bounded workstream. Inventory before editing, classify existing rules, preserve project-specific/stricter invariants, discover native tests and CI, prefer tools/adopt.py for mechanical bootstrap, do not overwrite existing files blindly, and require deterministic adoption validation before reporting PASS.
